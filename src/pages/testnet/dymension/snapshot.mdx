---
layout: '~/layouts/MarkdownLayout.astro'
title: Snapshot
logo: https://pbs.twimg.com/profile_images/1594732521962541058/NGdT4O2k_400x400.jpg
network: Testnet
chain: 35-c
version:  v0.1.1
---
- update 24/1

## Intruction

### install lz4
```
sudo apt install lz4 -y
```
## Stop Acre and Backup priv_validator_state.json
```
sudo systemctl stop dymd 
cp $HOME/.dymension/data/priv_validator_state.json $HOME/.dymension/priv_validator_state.json.backup
rm -rf $HOME/.dymension/data
```

### download data

```
curl -L https://snapshot.dymension.spt-node.xyz/dymension/acre-snapshot-20230207.tar.lz4 | lz4 -dc - | tar -xf - -C $HOME/.dymension
mv $HOME/.dymension/priv_validator_state.json.backup $HOME/.dymension/data/priv_validator_state.json
```

### start node

```
sudo systemctl restart dymd 
sudo journalctl -fu dymd  -o cat
```
